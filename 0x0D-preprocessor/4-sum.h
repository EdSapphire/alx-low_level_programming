#ifndef SUM_H
#define SUM_H
#include <stdio.h>
/**
 * define - Header file that writes a function-like macro SUM(x, y)
 * that computes the sum of the numbers x and y
 *
 */

#define SUM(X, Y) ((X) + (Y))
#endif
