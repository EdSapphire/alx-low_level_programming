#include <stdio.h>

/**
 * main - Declare a variable and assign interger value
 * increment the value
 * print the variable
 *
 * Return: Always 0.
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
